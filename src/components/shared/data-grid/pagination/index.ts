/**
 * Data-grid pagination: use GridToolbar (showPagination) or
 * these building blocks with grid.pagination / grid.actions.
 */
export {
    PageInfo,
    PageSizeSelect,
    PageNavigator,
} from '@/components/shared/data-table/pagination';
